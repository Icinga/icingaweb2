<?php


if ($this->auth()->isAuthenticated()) {
    echo $this->partial(
       'parts/menu.phtml',
        array(
            'items' => $menu->getChildren(),
            'url'   => $url
        )
    );
}

