[local-mysql]
type        = ido
resource    = ido-mysql

[local-pgsql]
type        = ido
resource    = ido-pgsql

[locallive]
disabled    = 1
type        = livestatus
resource    = livestatus

;[localfailsafe]
;enabled=false
;type = combo
;backends = localdb, locallive, localfile
